# 学习通待办

一个供个人日常使用的学习通作业与考试待办工具，运行在 Windows 和 Android 上。它替用户盯着即将截止的事项，并在合适的时机提醒。

## Language

### 待办与来源

**待办事项**：
一件有截止时间、需要用户完成的学习任务，包括作业、考试和测验。
_Avoid_：任务、item、todo

**来源**：
一件待办事项被发现的途径。同一件待办事项可以从多个来源被发现，但对用户只呈现一次，也只提醒一次。
_Avoid_：数据源、渠道

**受监控课程**：
用户明确选定、每轮同步都要检查的课程。未被选中的课程不参与扫描，因此它们的待办事项只能通过其他来源被发现。
_Avoid_：关注课程、订阅课程

**已见通知**：
在过去某一轮同步的通知列表里出现过的通知。通知一旦发出内容就不再变化，所以详情解析成功的已见通知会连标题、发出时间、正文与任务入口链接一并记下，后续同步直接复用、不再请求它的详情页；只是露过面、或详情抓取失败的，下一轮照常重试——把失败记成已解析会让那条通知的待办永久进不来。

通知列表按时间倒序返回，因此某一页里出现已见通知，就意味着后面全是更早的通知，翻页可以就此停下。停下不等于放弃那些通知：它们记录里的任务入口链接会照常参与本轮，任务详情页逐个重取，所以待办与不停下时一致。

记录条数有上限，越早见到的越先被淘汰，淘汰只会让那条通知重新抓一次详情。
_Avoid_：已读通知、通知缓存

### 提醒

**提醒规则**：
一条「在距离截止还剩多久时提醒一次」的约定。每条规则对同一件待办事项最多触发一次。
_Avoid_：提醒策略、提醒配置

**提醒去重键**：
判定「这次提醒是否已经发过」的依据，由待办事项、提醒规则和当时的截止时间三者共同构成。截止时间被推迟或提前后，同一条规则会重新算作未提醒。
_Avoid_：提醒 ID、通知 key

### 失效与信任

**静默失效**：
工具表面上在正常工作、界面没有任何异常，实际却已经漏掉了本该出现的待办事项或本该发出的提醒。它比明显报错更危险，因为用户不会去核对。工具的取舍原则是：宁可犯可见的错，不可犯静默的漏。
_Avoid_：静默错误、无声失败

**认证失效**：
用户的学习通登录态不再有效，工具无法再代表用户读取数据。这一状态必须对用户可见，因为它会让同步停在原地却不产生任何新信息，是静默失效最常见的成因。
_Avoid_：Cookie 过期、掉线、未登录

**URL 信任分级**：
按照「能否携带用户凭据访问」「能否交给系统浏览器打开」对目标地址划分的等级。学习通之外的地址、非加密地址和畸形地址都不属于可信级别，工具既不会带着凭据请求它们，也不会让它们变成可点击的外链。
_Avoid_：白名单、域名校验

### 平台

**平台角色**：
Windows 与 Android 各自独立完成抓取、存储和提醒，互不通信、互不依赖。代价是同一条提醒可能在两端各响一次——这是刻意接受的，用来换取任何一端单独安装即可使用，不需要配置服务端或配对设备。

两端的提醒能力并不对称：Android 能区分提醒的打扰强度，而 Windows 只有一种系统通知强度，因此在 Windows 上不同强度的提醒表现相同。这一不对称是刻意保留的，工具不会为了对齐而削弱 Android 一侧。
_Avoid_：端、客户端、设备
